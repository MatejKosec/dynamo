# Compute environment

_Auto-generated by `bin/probe-compute.sh` at workflow start._

## Local GPUs (visible inside this container)

```
0, NVIDIA RTX A6000, 49140 MiB, 26294 MiB
1, NVIDIA RTX A6000, 49140 MiB, 26299 MiB
```

**Local GPUs are available.** Prefer local execution for any work
item that fits these devices: use `mcp__bash-mcp__run` with
`docker build`, `docker run --gpus all ...`, `nvidia-smi`,
and direct `python -m dynamo.*` invocations. Local is
**much faster** than SSH-to-cluster and does not consume cluster
credits. Do NOT call `mcp__compute-session__*` when the work
fits a local GPU — only reach for it when the target hardware
is unavailable locally (e.g., B200, MI300X, multi-node).

## Remote clusters (reachable via SSH)

- _No remote clusters reachable._

**ssh-out appears blocked or no creds are configured.**
`mcp__compute-session__*` calls will fail at the network
layer. Do not retry them — use local execution only.

_Probed but unreachable_: computelab nscale prenyx

## Tools available in this container

- `python3` → `/usr/bin/python3`
- `git` → `/usr/bin/git`
- `gh` → `/usr/bin/gh`
- `nvidia-smi` → `/usr/bin/nvidia-smi`
- `node` → `/usr/local/bin/node`
